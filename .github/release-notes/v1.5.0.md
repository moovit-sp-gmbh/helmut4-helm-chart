## Linux clients no longer lose work when they scale down

Scale-down, `kubectl drain` and rollouts all delete pods, and nothing told the Linux client that
a render was about to be cut short. Autoscaling was therefore only safe for short jobs.

Every client pod now carries a `preStop` hook that holds SIGTERM while the container is still
working, so Kubernetes waits for the job instead of killing it. On by default.

```yaml
linuxClients:
  gracefulShutdown:
    enabled: true
    cpuThresholdMillicores: 200   # below this the client counts as idle
    pollSeconds: 10
    maxWaitSeconds: 3600          # hard cap, so a wedged client can't block a drain
```

Two details that matter more than they look:

- **CPU is read from the container's own cgroup**, not from `top` or `/proc`. Both of those
  report the *node's* load rather than the pod's, and the client image is Alpine — busybox `top`
  has no `Cpu(s)` line, so the usual `top -bn1 | grep "Cpu(s)"` recipe produces a nonsense
  number in it and the hook would never release early.
- **`terminationGracePeriodSeconds` is derived**, not configured: it is raised to
  `maxWaitSeconds + 30` whenever it would otherwise be smaller. A grace period shorter than the
  drain means the kubelet SIGKILLs the pod mid-job and the hook accomplishes nothing — so that
  combination is now impossible to configure.

The trade-off is deliberate: a pod can sit in `Terminating` for as long as its current job. That
makes draining a busy node slow, and with `maxSurge: 0` a rollout of N replicas serialises to
N × job length. Roll out when the fleet is quiet, or set `gracefulShutdown.enabled: false` to
get the old behaviour.

Also in this release: per-type `autoscaling.enabled: false` is now honoured (it was silently
overridden by the global setting), and every client type gets a CPU request by default — without
one, its HPA reported `<unknown>` and never scaled.

Setup guide: [docs/LINUX-CLIENTS.md](https://github.com/moovit-sp-gmbh/helmut4-helm-chart/blob/main/docs/LINUX-CLIENTS.md#graceful-shutdown)
