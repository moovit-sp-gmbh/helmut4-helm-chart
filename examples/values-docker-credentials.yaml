# Example: Docker Registry credentials
# Default credentials for repo.moovit24.de:443 (the public Helmut4 image registry).

docker:
  registry: "repo.moovit24.de:443"
  username: "moovit"
  password: "public"
  email: "your-email@example.com"
